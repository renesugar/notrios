{
  "schema": "notrios.docfeatures.registry.v1",
  "features": [
    {
      "id": "write-notes",
      "title": "Write and edit notes",
      "summary": "Create a note, change it, add to either end of it, and delete it. Deleting moves a note to Trash, and Trash is a place you can look in and take things back out of, not a countdown.",
      "cli": [
        "notriosctl notes create",
        "notriosctl notes edit",
        "notriosctl notes delete",
        "notriosctl notes restore"
      ],
      "rest": [
        "POST /api/v1/documents",
        "PUT /api/v1/documents/{document_id}",
        "PATCH /api/v1/documents/{document_id}",
        "DELETE /api/v1/documents/{document_id}",
        "POST /api/v1/documents/{document_id}/append",
        "POST /api/v1/documents/{document_id}/prepend",
        "GET /api/v1/trash",
        "DELETE /api/v1/trash/{document_id}",
        "POST /api/v1/trash/{document_id}/restore",
        "POST /api/v1/documents/{document_id}/revisions/{revision_id}/restore"
      ],
      "mcp": [
        "create_note",
        "update_note",
        "edit_note",
        "append_to_note",
        "prepend_to_note",
        "delete_note"
      ],
      "gui": [
        "Create a note in a notebook — executed (new-note-in-notebook)",
        "Edit and save a note — executed (edit-save)",
        "Restore a note from Trash — executed (trash-restore)",
        "Move this note to the Trash",
        "Restore",
        "Permanently delete this note and its revisions"
      ],
      "surface_note": "The command line writes, edits, files, trashes and restores a note; appending and prepending to one stay on REST and MCP. `notes create` exists so a note can be the end of a pipeline, and `notes delete` ships with `notes restore` because a delete whose undo lives on another surface is a poor boundary. This entry said editing and deleting happened only in the GUI or over REST/MCP for five days after both commands shipped, because neither appeared in `notriosctl help` and the coverage check reads the help."
    },
    {
      "id": "read-notes",
      "title": "Read a note and its structure",
      "summary": "Fetch a note whole, or just its body, its outline, its blocks, a line range, or an earlier revision. The structured views exist so a tool can work on part of a note without re-parsing all of it.",
      "rest": [
        "GET /api/v1/documents/{document_id}",
        "GET /api/v1/documents/{document_id}/body",
        "GET /api/v1/documents/{document_id}/blocks",
        "GET /api/v1/documents/{document_id}/lines",
        "GET /api/v1/documents/{document_id}/outline",
        "GET /api/v1/documents/{document_id}/revisions",
        "GET /api/v1/documents/{document_id}/revisions/{revision_id}"
      ],
      "mcp": [
        "get_document",
        "get_documents",
        "get_document_blocks",
        "get_document_outline",
        "get_note_line_range"
      ],
      "gui": [
        "All notes",
        "Search query"
      ],
      "surface_note": "The command line reads a note's metadata, and its body with `--body`. It reads no more than that yet: the outline, blocks, line ranges, attachments and links a note is made of are reachable over REST and MCP only, and v0.8 H21 adds them. This entry previously read \"No command line: reading a note is what the GUI and the API are for\", which was wrong twice -- `notes show` already existed, and nobody had decided a terminal should not read. An absent adapter tends to acquire a justification.",
      "cli": [
        "notriosctl notes show"
      ]
    },
    {
      "id": "search-notes",
      "title": "Search your notes",
      "summary": "Find notes by text, tag, notebook, date and the rest of the query language, across the library or within one note.",
      "rest": [
        "GET /api/v1/search",
        "POST /api/v1/search",
        "GET /api/v1/documents/{document_id}/search-in"
      ],
      "mcp": [
        "search_documents",
        "search_in_note"
      ],
      "gui": [
        "Page through search results — executed (search-page)"
      ],
      "surface_note": "No command line search command; `notriosctl export archive --query` applies the same language to an export instead. A search now spans every collection; `collection:\"id\"` narrows it to one provenance."
    },
    {
      "id": "saved-searches",
      "title": "Notebooks that are really saved searches",
      "summary": "Save a query as a notebook, so a view that would otherwise be retyped becomes something you open.",
      "rest": [
        "GET /api/v1/search-notebooks",
        "POST /api/v1/search-notebooks",
        "DELETE /api/v1/search-notebooks/{search_notebook_id}"
      ],
      "mcp": [
        "list_search_notebooks"
      ],
      "surface_note": "Made with `notebooks create --query` at the command line. In the GUI it is an action on a search that has already run and returned something, rather than a form: the query is one you have watched work, and a notebook made from an unrun query opens empty as easily as it opens right. The query is shown when keeping it and is not editable there, which is what keeps this from being a query form in another place.",
      "cli": [
        "notriosctl notebooks create"
      ],
      "gui": [
        "Keep this search — an action on a search that has run"
      ]
    },
    {
      "id": "organise-notebooks",
      "title": "Organise notes into notebooks",
      "summary": "Make notebooks, nest them, move notes between them, and see what a notebook deletion would take with it before agreeing to it.",
      "cli": [
        "notriosctl notebooks create",
        "notriosctl notebooks list",
        "notriosctl notes move"
      ],
      "rest": [
        "GET /api/v1/notebooks",
        "POST /api/v1/notebooks",
        "GET /api/v1/notebooks/tree",
        "GET /api/v1/notebooks/{notebook_id}",
        "PATCH /api/v1/notebooks/{notebook_id}",
        "DELETE /api/v1/notebooks/{notebook_id}",
        "GET /api/v1/notebooks/{notebook_id}/deletion-preview",
        "GET /api/v1/notebooks/{notebook_id}/notes",
        "POST /api/v1/documents/{document_id}/notebook"
      ],
      "mcp": [
        "list_notebooks",
        "get_notebook_tree",
        "get_notebook_notes",
        "move_note_to_notebook"
      ],
      "gui": [
        "Choose a notebook for a new note — executed (new-note-in-notebook)",
        "Review a notebook deletion — executed (delete-notebook-review)"
      ],
      "surface_note": "`notebooks list` prints a table of ids and names, with `--json` for the structured form. It printed JSON only until v0.8 H22, which is the right answer for a script and the wrong one for the person the command exists to help: a query names a notebook by id, and finding the id meant reading a JSON object at a terminal."
    },
    {
      "id": "tag-a-note",
      "title": "Tag and untag a note",
      "summary": "Attach a tag to a note, take one off, and see what a note carries. Tags are hierarchical: `field/dusk` sits under `field`.",
      "cli": [
        "notriosctl tags add",
        "notriosctl tags remove",
        "notriosctl tags list"
      ],
      "rest": [
        "POST /api/v1/documents/{document_id}/tags/{tag}",
        "DELETE /api/v1/documents/{document_id}/tags/{tag}"
      ],
      "mcp": [
        "tag_note",
        "untag_note"
      ],
      "gui": [
        "tag-editor-trigger",
        "tag-editor-input",
        "tag-editor-add",
        "tag-editor-list"
      ]
    },
    {
      "id": "browse-and-rename-tags",
      "title": "See and rename tags",
      "summary": "List the tags in a library, see a note's tags, and rename a whole tag hierarchy at once.",
      "cli": [
        "notriosctl tags rename"
      ],
      "rest": [
        "GET /api/v1/tags",
        "POST /api/v1/tags/rename",
        "GET /api/v1/documents/{document_id}/tags"
      ],
      "mcp": [
        "list_tags"
      ],
      "surface_note": "Renaming is offered beside the tag, because it is decided while looking at the tag and the count next to it is half the reason for deciding. The dry run is not a prediction: the service performs the rename in a transaction and rolls it back, so the report and the apply cannot disagree. Applying is unreachable until a report of that exact rename has been shown, and editing the name or the children option withdraws it.",
      "gui": [
        "Tags in the sidebar with note counts",
        "Rename the tag — beside each tag",
        "Show me what changes — the dry run",
        "Rename — applies what the report described"
      ]
    },
    {
      "id": "collections",
      "title": "Group libraries into collections",
      "summary": "Collections sit above notebooks and are how an import keeps its material together.",
      "rest": [
        "GET /api/v1/collections",
        "POST /api/v1/collections",
        "GET /api/v1/collections/{collection_id}",
        "PATCH /api/v1/collections/{collection_id}"
      ],
      "mcp": [
        "list_collections"
      ],
      "surface_note": "A collection is provenance, not a place notes live: notes are imported into a notebook, which is what a person browses, and carry a collection identifier saying where they came from. Search spans every collection and `collection:` narrows it -- it used to be pinned to one collection chosen by the caller and defaulted to `default`, which made imported notes unfindable and the field meaningless. The inspector names the collection only when it is not `default`, since a row saying so on every note written here would say nothing. The command line lists collections and shows one, with how many notes name each; REST and MCP list them without that count, which H16 should decide about along with the rest of the collection shape. Creating and reconfiguring a collection is still not on the command line: an import creates one silently when `--collection` names a new id, and this entry claimed the opposite for months. H16 decides what a collection's `kind` and capabilities mean, because the schema records neither, so neither is printed here.",
      "gui": [
        "Collection — shown in the note inspector when a note came from somewhere",
        "collection: in the search box"
      ],
      "cli": [
        "notriosctl collections list",
        "notriosctl collections show"
      ]
    },
    {
      "id": "attachments",
      "title": "Attach and manage files",
      "summary": "Add files to notes, read them back, see what references what, and find attachments nothing points at any more.",
      "cli": [
        "notriosctl resources report"
      ],
      "rest": [
        "POST /api/v1/resources",
        "GET /api/v1/resources/{resource_id}",
        "DELETE /api/v1/resources/{resource_id}",
        "GET /api/v1/resources/{resource_id}/content",
        "GET /api/v1/resources/reports/reference",
        "GET /api/v1/documents/{document_id}/resources",
        "POST /api/v1/documents/{document_id}/resources/{resource_id}",
        "DELETE /api/v1/documents/{document_id}/resources/{resource_id}"
      ],
      "mcp": [
        "list_document_resources",
        "read_resource"
      ],
      "surface_note": "Measured by the H15 control crawl, which found the upload field in the note inspector and the Attachments tab. This was recorded as absent from the GUI until the crawl opened a note; no journey covers it yet.",
      "gui": [
        "Upload image/PDF/resource — the file field in the note inspector",
        "Attachments — a sync centre tab"
      ]
    },
    {
      "id": "remote-media",
      "title": "Bring remote images into the library",
      "summary": "Find images a note points at on the web, check them against the domain policy, and copy the allowed ones in so the note stops depending on somebody else's server.",
      "cli": [
        "notriosctl localize"
      ],
      "rest": [
        "POST /api/v1/documents/{document_id}/remote-media/scan",
        "POST /api/v1/documents/{document_id}/remote-media/localize",
        "GET /api/v1/media-policy",
        "POST /api/v1/media-policy/check-url"
      ],
      "mcp": [
        "scan_remote_media",
        "localize_remote_media",
        "request_resource_fetch"
      ],
      "surface_note": "Measured by the H15 control crawl on a note seeded with an image on an allowed domain. The earlier note here said localizing was a maintenance action rather than an editing one; it is in the editor.",
      "gui": [
        "Remote media list — shown on a note that points at images on the web",
        "Localize allowed media"
      ]
    },
    {
      "id": "links-between-notes",
      "title": "Link notes to each other",
      "summary": "Get a stable link to a note or one of its sections, open one, check the links in a draft before saving, and register Notrios as the handler for notrios:// links.",
      "cli": [
        "notriosctl link",
        "notriosctl open",
        "notriosctl register-url-handler"
      ],
      "rest": [
        "GET /api/v1/links/suggest",
        "POST /api/v1/links/check",
        "POST /api/v1/links/resolve",
        "GET /api/v1/documents/{document_id}/links"
      ],
      "mcp": [
        "list_document_links"
      ],
      "gui": [
        "Insert a link and check it — executed (insert-and-check-link)"
      ]
    },
    {
      "id": "graph",
      "title": "See the shape of the link graph",
      "summary": "Look at what surrounds a note, find a path between two notes, and get a report on orphans, isolates and hubs.",
      "cli": [
        "notriosctl graph report",
        "notriosctl graph export"
      ],
      "rest": [
        "POST /api/v1/graph",
        "POST /api/v1/graph/path",
        "GET /api/v1/graph/report",
        "POST /api/v1/graph/report/note"
      ],
      "mcp": [
        "get_graph",
        "get_graph_report",
        "find_graph_path"
      ],
      "gui": [
        "Reports"
      ]
    },
    {
      "id": "templates-and-tasks",
      "title": "Templates and tasks",
      "summary": "Keep note templates and make new notes from them, and see the tasks across a library.",
      "rest": [
        "GET /api/v1/templates",
        "GET /api/v1/templates/{document_id}",
        "POST /api/v1/templates/{document_id}/create",
        "GET /api/v1/tasks"
      ],
      "mcp": [
        "list_templates",
        "create_from_template",
        "list_tasks"
      ],
      "surface_note": "A template is an ordinary note carrying a note-template block, and a task is a checkbox line in a note tagged `task` or `todo`; neither is a table, and both are read back from the Markdown. So nothing creates a task -- writing `- [ ] chase the permit` into a note is how one comes to exist, which `notes create` already does -- and the command line covers the other half: asking what remains, and instantiating a template, which is the repeatable version of the same workflow. A missing placeholder is refused rather than left blank, so a template that gains a field fails the scripts that do not know about it. The tag is required because a checkbox is ordinary Markdown and appears in quoted examples; `--untagged` asks for those too. No GUI journey yet.",
      "cli": [
        "notriosctl tasks list",
        "notriosctl templates list",
        "notriosctl templates create"
      ]
    },
    {
      "id": "query-blocks",
      "title": "Live query blocks inside a note",
      "summary": "Put a query in a note and have its results render where it sits.",
      "rest": [
        "POST /api/v1/note-queries/run"
      ],
      "mcp": [
        "run_note_query"
      ],
      "surface_note": "No command line. The GUI renders them where they sit: the preview finds each ```note-query block, runs it through the same parser the search box uses, and fills the block with the result. This row read as a gap for as long as the control crawl was the only measurement, and the crawl cannot see it and never could -- a query block renders content, not a control, and the crawl enumerates interactive elements. Its evidence is web/src/note-query.ts and its tests instead.",
      "gui": [
        "Query blocks render in the preview, in place"
      ]
    },
    {
      "id": "batch-operations",
      "title": "Do many organiser operations at once",
      "summary": "Send a batch of moves, tags and notebook changes as one request, so a large reorganisation is one reviewable action.",
      "rest": [
        "POST /api/v1/batch"
      ],
      "mcp": [
        "run_batch"
      ],
      "surface_note": "No command line or GUI journey; batching is for tools."
    },
    {
      "id": "import-notes",
      "title": "Import from another application",
      "summary": "Bring in a Joplin raw export, an Obsidian vault, a Twitter or X archive, a ChatGPT or Claude export, or a Notrios archive. Every importer dry-runs first.",
      "cli": [
        "notriosctl import joplin-raw",
        "notriosctl import obsidian",
        "notriosctl import twitter",
        "notriosctl import chatgpt",
        "notriosctl import claude",
        "notriosctl import archive"
      ],
      "surface_note": "In the desktop app only, through the native bridge: these operations name a folder on the machine running the library, and no REST route starts them. In a browser the Import/Export control is shown and disabled with that reason rather than hidden. Joplin and Obsidian can be scanned first without writing. Importing from Notrios verifies the archive without opening a database, and merges only: replace, fork and adopt stay on the command line because they change which library this is. Twitter, ChatGPT and Claude imports remain command line only. The journey is photographed by the desktop harness rather than by the browser capture, because the dialog it lives in is correctly disabled in a browser: the real application is driven under Xvfb, and each step declares the line it expects in the application's own transcript, so a keystroke that lands elsewhere fails the capture instead of producing a confident picture of the wrong thing.",
      "gui": [
        "Import from Joplin",
        "Import from Obsidian",
        "Import from Notrios"
      ]
    },
    {
      "id": "export-and-archive",
      "title": "Export your library",
      "summary": "Write a portable archive of everything or a chosen subset, check one for compatibility, verify one, and restore one with an explicit intent.",
      "cli": [
        "notriosctl export archive",
        "notriosctl export archive-v2",
        "notriosctl verify archive-v2",
        "notriosctl restore archive-v2",
        "notriosctl compatibility archive-v2"
      ],
      "surface_note": "In the desktop app only, through the native bridge: these operations name a folder on the machine running the library, and no REST route starts them. In a browser the Import/Export control is shown and disabled with that reason rather than hidden. The GUI writes a complete archive; exporting a subset by notebook, tag or query stays on the command line, because choosing a subset means seeing what it selects first. The journey is photographed by the desktop harness rather than by the browser capture, because the dialog it lives in is correctly disabled in a browser: the real application is driven under Xvfb, and each step declares the line it expects in the application's own transcript, so a keystroke that lands elsewhere fails the capture instead of producing a confident picture of the wrong thing.",
      "gui": [
        "Export this library",
        "Verify the archive"
      ]
    },
    {
      "id": "snapshots",
      "title": "Back up and restore the whole library",
      "summary": "Take a physical snapshot of the database and its attachments, verify it, and restore it — replacing this library or adopting the snapshot as a new replica.",
      "cli": [
        "notriosctl snapshot create",
        "notriosctl snapshot verify",
        "notriosctl snapshot restore"
      ],
      "surface_note": "In the desktop app only, through the native bridge: these operations name a folder on the machine running the library, and no REST route starts them. In a browser the Import/Export control is shown and disabled with that reason rather than hidden. Creating a snapshot is in the GUI; restoring one is not, because a restore replaces the library the window is showing. The journey is photographed by the desktop harness rather than by the browser capture, because the dialog it lives in is correctly disabled in a browser: the real application is driven under Xvfb, and each step declares the line it expects in the application's own transcript, so a keystroke that lands elsewhere fails the capture instead of producing a confident picture of the wrong thing.",
      "gui": [
        "Take a snapshot"
      ]
    },
    {
      "id": "sync-pairing",
      "title": "Pair two of your own libraries",
      "summary": "Enrol a library for synchronization, issue a single-use code, and spend it from the other side so the two learn each other's keys.",
      "cli": [
        "notriosctl sync init",
        "notriosctl sync invite",
        "notriosctl sync join",
        "notriosctl sync accept",
        "notriosctl sync enroll",
        "notriosctl sync discover",
        "notriosctl sync handshake"
      ],
      "rest": [
        "GET /api/v1/sync-ui",
        "POST /api/v1/sync-ui/initialize",
        "PUT /api/v1/sync-ui/configuration",
        "POST /api/v1/sync-ui/discover",
        "POST /api/v1/sync-ui/invitations",
        "POST /api/v1/sync-ui/pair",
        "GET /api/v1/sync/handshake",
        "POST /api/v1/sync/pair"
      ],
      "gui": [
        "Open the sync center — executed (open-sync-center)"
      ]
    },
    {
      "id": "sync-exchange",
      "title": "Synchronize with a replica",
      "summary": "Exchange changes with a paired replica, directly over an authenticated connection or through a folder you both can reach — a second drive, or a cloud folder mapped locally.",
      "cli": [
        "notriosctl sync exchange",
        "notriosctl sync once",
        "notriosctl sync status",
        "notriosctl sync start",
        "notriosctl sync retention"
      ],
      "rest": [
        "GET /api/v1/sync/status",
        "GET /api/v1/sync/carrier/namespaces",
        "GET /api/v1/sync/carrier/{namespace}/{class}/{name}",
        "GET /api/v1/sync/carrier/{segment1}/{segment2}",
        "PUT /api/v1/sync/carrier/{segment1}/{segment2}",
        "DELETE /api/v1/sync/carrier/{segment1}/{segment2}",
        "POST /api/v1/jobs/sync/plan",
        "POST /api/v1/jobs/sync/start",
        "GET /api/v1/jobs/{job_id}/sync"
      ],
      "mcp": [
        "start_sync",
        "plan_sync",
        "get_sync_status"
      ],
      "gui": [
        "Sync now"
      ]
    },
    {
      "id": "sync-peers",
      "title": "See and end trust between replicas",
      "summary": "List the replicas a library trusts, revoke one's key, and retire a peer after reviewing exactly what retiring it means.",
      "cli": [
        "notriosctl sync peers",
        "notriosctl sync revoke",
        "notriosctl sync retire"
      ],
      "rest": [
        "POST /api/v1/sync-ui/peers/{replica_id}/retire",
        "POST /api/v1/sync-ui/peers/{replica_id}/retirement-preview",
        "POST /api/v1/sync-ui/peers/{replica_id}/snapshot-permission",
        "GET /api/v1/sync-ui/retention"
      ],
      "gui": [
        "Peers",
        "Retention"
      ]
    },
    {
      "id": "sync-recovery",
      "title": "Recover a replica and resolve conflicts",
      "summary": "Fetch a peer-verified backup, inspect one without installing it, work through sync conflicts, and decide what a shared attachment should do.",
      "cli": [
        "notriosctl sync fetch-backup"
      ],
      "rest": [
        "GET /api/v1/sync-conflicts",
        "GET /api/v1/sync-ui/conflicts/{conflict_id}",
        "POST /api/v1/sync-ui/conflicts/{conflict_id}/resolve",
        "POST /api/v1/sync-ui/recovery",
        "POST /api/v1/sync-ui/resources/{resource_id}/intent",
        "POST /api/v1/sync-ui/backups",
        "POST /api/v1/sync-ui/backups/inspect",
        "GET /api/v1/sync/backups/{backup_id}",
        "POST /api/v1/sync/backups"
      ],
      "mcp": [
        "list_sync_conflicts"
      ],
      "gui": [
        "Conflicts",
        "Backup & recovery"
      ]
    },
    {
      "id": "sync-credentials",
      "title": "Choose where sync keys are kept",
      "summary": "An installed Notrios keeps the key that protects your sync material in the operating system's credential store. Move existing keys between that and the owner-only development file, in either direction.",
      "cli": [
        "notriosctl sync migrate-credentials"
      ],
      "surface_note": "Deliberately command line only: this item forbids a credential-management REST or MCP surface."
    },
    {
      "id": "jobs",
      "title": "Watch and steer long-running work",
      "summary": "Imports, exports and syncs run as jobs you can list, inspect, cancel, retry and reset.",
      "cli": [
        "notriosctl jobs list",
        "notriosctl jobs show",
        "notriosctl jobs status",
        "notriosctl jobs cancel",
        "notriosctl jobs retry"
      ],
      "rest": [
        "GET /api/v1/jobs",
        "GET /api/v1/jobs/{job_id}",
        "POST /api/v1/jobs/{job_id}/cancel",
        "POST /api/v1/jobs/{job_id}/retry",
        "POST /api/v1/jobs/{job_id}/reset"
      ],
      "mcp": [
        "list_jobs",
        "get_job",
        "cancel_sync_job",
        "retry_sync_job"
      ],
      "surface_note": "Measured by the H15 control crawl once jobs were seeded, which is what the row had been waiting for: every job control is gated on state, so an interface holding only completed jobs shows none of them. What the GUI has is the sync centre's list of recent *sync* jobs, with Cancel on one still running or queued and Retry on one that failed or was cancelled. Import, export and snapshot jobs appear nowhere: the sync centre lists four sync kinds and only those, and the import dialog shows a final report rather than a job. There is no general job list, no inspect and no reset.",
      "gui": [
        "Recent synchronization jobs — kind, state and progress",
        "Cancel — while a sync job is running or queued",
        "Retry — on a sync job that failed or was cancelled"
      ]
    },
    {
      "id": "profiles",
      "title": "Keep separate libraries",
      "summary": "Run more than one library on a machine — personal notes, work notes, a blog — each with its own database, its own settings and its own address.",
      "cli": [
        "notriosctl profile create",
        "notriosctl profile list",
        "notriosctl profile show",
        "notriosctl profile register",
        "notriosctl profile forget",
        "notriosctl profile start",
        "notriosctl profile validate"
      ],
      "surface_note": "Switching between profiles is in the GUI; creating, registering and forgetting them is not. A profile registry is about this machine, which the desktop app also runs on, so the rest is a gap rather than a boundary; the browser mode is the part that genuinely cannot manage it.",
      "gui": [
        "Active profile — the selector in the sync centre's Setup tab"
      ]
    },
    {
      "id": "publishing",
      "title": "Publish a subset of your notes",
      "summary": "Choose what leaves the library, review exactly what a publication would include and withhold, save that choice as a profile, and publish only after agreeing to the reviewed plan.",
      "cli": [
        "notriosctl publish plan",
        "notriosctl publish run",
        "notriosctl publish profile save",
        "notriosctl publish profile list",
        "notriosctl publish profile delete"
      ],
      "rest": [
        "POST /api/v1/selection/plan"
      ],
      "mcp": [
        "plan_selection"
      ],
      "surface_note": "The review is in the GUI, because reading what a publication lets out is the point of the feature rather than a step before it: the counts lead with what is withheld and which links get rewritten, not with what is sent. Planning goes through the native bridge rather than over REST for a correctness reason, not a path one -- `publish run` re-plans and refuses unless the digest matches what was reviewed, so the plan shown must be the plan the run recomputes, and an equivalent selection rebuilt over REST would match only by luck. Profiles are read and never written here: a profile carries a target, a selection and a privacy policy, and a form that quietly defaulted one is how something private gets published, so authoring stays `notriosctl publish profile save`. The journey is photographed by the desktop harness rather than by the browser capture, because the dialog it lives in is correctly disabled in a browser: the real application is driven under Xvfb, and each step declares the line it expects in the application's own transcript, so a keystroke that lands elsewhere fails the capture instead of producing a confident picture of the wrong thing.",
      "gui": [
        "Publish a subset of your notes — choosing a saved profile",
        "Review what this would publish",
        "Publish this review — in the desktop application only"
      ]
    },
    {
      "id": "maintenance",
      "title": "Keep a library healthy",
      "summary": "Find what has rotted, fix what can be fixed mechanically, reclaim space, and check that this installation is set up the way you think it is.",
      "cli": [
        "notriosctl lint",
        "notriosctl fix",
        "notriosctl gc",
        "notriosctl doctor",
        "notriosctl paths",
        "notriosctl config show",
        "notriosctl version",
        "notriosctl seed-help"
      ],
      "rest": [
        "GET /api/v1/admin/lint/report",
        "GET /api/v1/admin/gc/report",
        "GET /api/v1/status",
        "GET /healthz"
      ],
      "mcp": [
        "get_lint_report"
      ],
      "surface_note": "The GUI reports both, and repairs. Repairing goes through the native bridge because the lint surface is deliberately read-only over REST -- that is a remote, programmatic surface an agent reaches, and repairs to somebody's notes should not be one call away from one -- which says nothing about a person pressing a button in the program that owns the library. Each repair writes a revision against a required base revision, so a note edited since the plan refuses rather than being repaired against text nobody read, and refusals are reported per note. Collecting stays `notriosctl gc --apply`: it deletes blobs, and there is no revision to go back to. doctor, paths, config show and seed-help remain command line.",
      "gui": [
        "Health — the lint report and the collection plan",
        "Show me what would be repaired",
        "Repair them — in the desktop application only"
      ]
    },
    {
      "id": "upgrade-layout",
      "title": "Move a pre-0.8 library into place",
      "summary": "A library that lived in ./data next to the program is relocated into the directories an installed Notrios uses, after showing you the plan.",
      "cli": [
        "notriosctl migrate"
      ],
      "surface_note": "Command line only, and genuinely so: it relocates the directories the running program uses, which is not something the program can sensibly do to itself while serving them."
    },
    {
      "id": "mcp-endpoint",
      "title": "Let an AI assistant use your library",
      "summary": "Notrios speaks MCP, so an assistant can read and, within a scope you grant, change your notes.",
      "rest": [
        "GET /mcp",
        "POST /mcp"
      ],
      "surface_note": "The endpoint itself has no command line or GUI; the tools it exposes are listed against the features above."
    }
  ]
}
