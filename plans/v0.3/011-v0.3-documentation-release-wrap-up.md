# v0.3 H11 — Documentation and release wrap-up

Status: completed 2026-07-26.

Model: GPT-5.6 (Codex).

## Goal

Close the v0.3 import/resource/media/large-library milestone with user
documentation that matches H5–H10, a deterministic offline Help reseed, an
auditable v0.3.0 release checklist, full validation, and a verified source
release archive.

## Changes

- Added `docs/operations.md`, a single user workflow for remote-media
  localization, exact resource/reference reports, retention-aware garbage
  collection, importer dry-run/checkpoint/resume, and Recoll status/repair.
  Added it to the documentation site navigation and Help source set.
- Reconciled the user/API/schema/security docs and feature matrix with the
  implemented schema-v11 H5–H10 behavior. Removed stale active labels and the
  stale claim that GET search was a placeholder.
- Bumped service, CLI, MCP, and web package version metadata to `0.3.0`.
- Added the v0.3.0 local release-candidate and repository-owner publishing
  sections to `RELEASE_CHECKLIST.md`.
- Marked v0.3 complete in the roadmap/status/handoff and drafted the next
  v0.4 plan from `ROADMAP.md`; v0.4 implementation remains unstarted pending
  user approval.

## Validation

- `go vet ./...`
- `go test ./...` (all Go packages)
- `python3 scripts/check_required_files.py`
- `python3 scripts/check_plan_loops.py`
- `bash scripts/validate-scaffold.sh`
- OpenAPI YAML parse and migration-copy equality
- `cd web && npm ci && npm run typecheck && npm test && npm run build`
  (5 files, 38 tests)
- `make gui`
- `bash scripts/build_docs_site.sh /tmp/notrios-h11-site` (11 indexed pages)
- two live `notriosctl seed-help` passes against a fresh database (11 created,
  then 11 kept)
- `bash scripts/mvp_smoke.sh`
- `bash scripts/run_performance_smoke.sh`
- 100-note Joplin RAW dry-run/interruption/resume profile
- 100-note Obsidian dry-run/interruption/resume profile
- 100-document native Recoll index/query/incremental-drift/reconciliation
  profile against Recoll 1.44.0/Xapian 1.4.22
- `bash scripts/package_release.sh /tmp/notrios-v0.3.0-h11-20260726.zip`
- `python3 scripts/check_release_zip.py /tmp/notrios-v0.3.0-h11-20260726.zip`

## v0.3 milestone result

H1–H11 are complete. The milestone delivered:

- one SSRF-resistant policy/quarantine/localization pipeline shared by
  REST/CLI/MCP/UI/importers;
- exact-hash resource reports, an inert-by-default review-only perceptual
  hook, and dry-run-first retention-aware GC;
- keyset/snapshot pagination with 10k/100k/500k profiles;
- deterministic, resumable, loss-preserving Joplin RAW and Obsidian importers;
- a bounded, retrying, self-reconciling, observable optional Recoll sidecar.

No GitHub push, merge, tag, or release publication was performed.
