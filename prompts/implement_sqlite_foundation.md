# Implement SQLite Foundation

Implement the next MVP slice for SQLite initialization and migration application. Keep the change minimal: open database, apply migrations, report schema/status via `/api/v1/status`, add tests, and update plan-status files.
