# Create New Plan From Roadmap

The current `PLAN.md` is complete. Archive it under `plans/` using the project convention. Read `ROADMAP.md`, choose the next milestone, and create a new `PLAN.md` with incremental tasks that leave the repository working after each step. Ask the user before implementing the new plan.
