# Review Scaffold

Review the scaffold against the copied conversation context and root documents. Look for contradictions, missing directories, missing validation commands, vague tasks, and choices that need user confirmation. Produce a patch that improves documentation without starting product implementation.
