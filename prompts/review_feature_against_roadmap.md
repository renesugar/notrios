Review the requested feature against `FEATURE_MATRIX.md`, `ROADMAP.md`, `PLAN.md`, and `SYSTEM_ARCHITECTURE.md`.

Return:

1. Whether the feature is MVP, soon, later, optional, or out of scope.
2. Which service owns the feature.
3. Which files need changes.
4. The smallest working-state slice that can implement or document the feature.
5. Any open questions to add to `agent/OPEN_QUESTIONS.md`.

Do not implement until the user confirms if the feature would alter the current active plan.
