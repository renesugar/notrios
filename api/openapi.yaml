openapi: 3.1.0
info:
  title: Notes Companion API
  version: 0.1.0-scaffold
  description: REST contract skeleton for the Notes Companion service.
servers:
  - url: http://127.0.0.1:8080
security: []
paths:
  /healthz:
    get:
      summary: Health check
      responses:
        '200':
          description: Service is alive
          content:
            text/plain:
              schema:
                type: string
  /api/v1/status:
    get:
      summary: Service status
      responses:
        '200':
          description: Status response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatusResponse'
  /mcp:
    get:
      summary: MCP MVP endpoint metadata
      responses:
        '200':
          description: MCP endpoint metadata
          content:
            application/json:
              schema: { type: object }
        '404':
          $ref: '#/components/responses/Error'
    post:
      summary: MCP MVP JSON-RPC endpoint
      description: Dependency-free read-only MCP MVP adapter supporting initialize, tools/list, and tools/call.
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object }
      responses:
        '200':
          description: JSON-RPC response
          content:
            application/json:
              schema: { type: object }
  /api/v1/collections:
    get:
      summary: List collections
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Cursor'
      responses:
        '200':
          description: Collection list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CollectionPage'
    post:
      summary: Create collection
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCollectionRequest'
      responses:
        '201':
          description: Created collection
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Collection'
  /api/v1/collections/{collection_id}:
    get:
      summary: Get collection
      parameters:
        - $ref: '#/components/parameters/CollectionID'
      responses:
        '200':
          description: Collection
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Collection'
        '404':
          $ref: '#/components/responses/Error'
    patch:
      summary: Update collection metadata
      parameters:
        - $ref: '#/components/parameters/CollectionID'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchCollectionRequest'
      responses:
        '200':
          description: Updated collection
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Collection'
  /api/v1/search:
    get:
      summary: Search documents with simple query parameters
      parameters:
        - name: q
          in: query
          schema: { type: string }
        - name: collection
          in: query
          schema: { type: string }
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Cursor'
      responses:
        '200':
          description: Search results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResponse'
    post:
      summary: Search documents
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SearchRequest'
      responses:
        '200':
          description: Search results
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SearchResponse'
        '400':
          $ref: '#/components/responses/Error'
  /api/v1/documents:
    post:
      summary: Create document
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DocumentMutationRequest'
      responses:
        '201':
          description: Created document
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Document'
  /api/v1/documents/{document_id}:
    get:
      summary: Get document
      parameters:
        - $ref: '#/components/parameters/DocumentID'
      responses:
        '200':
          description: Document
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Document'
    put:
      summary: Replace document body and metadata
      parameters:
        - $ref: '#/components/parameters/DocumentID'
        - $ref: '#/components/parameters/IfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DocumentMutationRequest'
      responses:
        '200':
          description: Updated document
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Document'
        '409':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
    patch:
      summary: Patch document metadata or apply surgical edits
      parameters:
        - $ref: '#/components/parameters/DocumentID'
        - $ref: '#/components/parameters/IfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DocumentPatchRequest'
      responses:
        '200':
          description: Updated document
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Document'
        '409':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
    delete:
      summary: Move document to trash
      parameters:
        - $ref: '#/components/parameters/DocumentID'
        - $ref: '#/components/parameters/IfMatch'
      responses:
        '204':
          description: Trashed
        '409':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
  /api/v1/documents/{document_id}/body:
    get:
      summary: Get document body text
      parameters:
        - $ref: '#/components/parameters/DocumentID'
      responses:
        '200':
          description: Document body
          content:
            text/markdown:
              schema: { type: string }
            text/plain:
              schema: { type: string }
  /api/v1/documents/{document_id}/revisions:
    get:
      summary: List document revisions
      parameters:
        - $ref: '#/components/parameters/DocumentID'
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Cursor'
      responses:
        '200':
          description: Revision page
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RevisionPage'
  /api/v1/documents/{document_id}/revisions/{revision_id}:
    get:
      summary: Get document revision
      parameters:
        - $ref: '#/components/parameters/DocumentID'
        - $ref: '#/components/parameters/RevisionID'
      responses:
        '200':
          description: Revision
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentRevision'
  /api/v1/documents/{document_id}/revisions/{revision_id}/restore:
    post:
      summary: Restore a revision by creating a new current revision
      parameters:
        - $ref: '#/components/parameters/DocumentID'
        - $ref: '#/components/parameters/RevisionID'
        - $ref: '#/components/parameters/IfMatch'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RestoreRevisionRequest'
      responses:
        '200':
          description: Restored document
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Document'
        '409':
          $ref: '#/components/responses/Error'
        '428':
          $ref: '#/components/responses/Error'
  /api/v1/documents/{document_id}/resources:
    get:
      summary: List resources referenced by a document
      parameters:
        - $ref: '#/components/parameters/DocumentID'
        - name: relation
          in: query
          schema: { type: string }
      responses:
        '200':
          description: Resource reference page
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceReferencePage'
  /api/v1/documents/{document_id}/resources/{resource_id}:
    post:
      summary: Attach an existing resource to a document
      parameters:
        - $ref: '#/components/parameters/DocumentID'
        - $ref: '#/components/parameters/ResourceID'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AttachResourceRequest'
      responses:
        '200':
          description: Resource reference
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResourceReference'
    delete:
      summary: Detach a resource from a document
      parameters:
        - $ref: '#/components/parameters/DocumentID'
        - $ref: '#/components/parameters/ResourceID'
      responses:
        '204':
          description: Detached
  /api/v1/resources:
    post:
      summary: Upload a resource
      description: MVP Task 4 accepts a raw request body. Supply filename and collection_id as query parameters or filename through Content-Disposition. Multipart form parsing is a future enhancement.
      parameters:
        - name: filename
          in: query
          schema: { type: string }
        - name: collection_id
          in: query
          schema: { type: string, default: default }
      requestBody:
        required: true
        content:
          application/octet-stream:
            schema: { type: string, format: binary }
      responses:
        '201':
          description: Created resource
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Resource'
  /api/v1/resources/{resource_id}:
    head:
      summary: Resource metadata headers
      parameters:
        - $ref: '#/components/parameters/ResourceID'
      responses:
        '200': { description: Metadata headers }
    get:
      summary: Get resource metadata
      parameters:
        - $ref: '#/components/parameters/ResourceID'
      responses:
        '200':
          description: Resource metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Resource'
    delete:
      summary: Delete or mark resource for garbage collection
      parameters:
        - $ref: '#/components/parameters/ResourceID'
      responses:
        '204': { description: Deleted when unreferenced }
        '409': { description: Resource is still referenced by at least one document }
  /api/v1/resources/{resource_id}/content:
    get:
      summary: Download resource bytes
      parameters:
        - $ref: '#/components/parameters/ResourceID'
        - name: download
          in: query
          schema: { type: boolean }
      responses:
        '200':
          description: Resource stream
          content:
            application/octet-stream:
              schema: { type: string, format: binary }
        '206':
          description: Future partial resource stream; range support is not implemented in the MVP Task 4 slice
  /api/v1/documents/{document_id}/links:
    get:
      summary: List document links and backlinks
      parameters:
        - $ref: '#/components/parameters/DocumentID'
        - name: direction
          in: query
          schema:
            type: string
            enum: [outgoing, incoming, both]
            default: both
      responses:
        '200':
          description: Link page
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentLinkPage'
  /api/v1/documents/{document_id}/outline:
    get:
      summary: Get document outline
      parameters:
        - $ref: '#/components/parameters/DocumentID'
      responses:
        '200':
          description: Outline
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentOutline'
  /api/v1/documents/{document_id}/remote-media/scan:
    post:
      summary: Scan a document for remote media URLs
      parameters:
        - $ref: '#/components/parameters/DocumentID'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RemoteMediaRequest'
      responses:
        '200':
          description: Remote media scan
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RemoteMediaResult'
  /api/v1/documents/{document_id}/remote-media/localize:
    post:
      summary: Download approved remote media and rewrite the document
      parameters:
        - $ref: '#/components/parameters/DocumentID'
        - $ref: '#/components/parameters/IfMatch'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RemoteMediaRequest'
      responses:
        '200':
          description: Localization result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RemoteMediaResult'
  /api/v1/documents/{document_id}/append:
    post:
      summary: Append text to a note (If-Match optional; retries once without it)
      parameters:
        - { name: document_id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: Updated document }
  /api/v1/documents/{document_id}/prepend:
    post:
      summary: Insert text at the beginning of a note
      parameters:
        - { name: document_id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: Updated document }
  /api/v1/documents/{document_id}/lines:
    get:
      summary: Read a 1-indexed inclusive line range of a note body
      parameters:
        - { name: document_id, in: path, required: true, schema: { type: string } }
        - { name: start, in: query, schema: { type: integer } }
        - { name: end, in: query, schema: { type: integer } }
      responses:
        '200': { description: Line slice }
  /api/v1/documents/{document_id}/search-in:
    get:
      summary: Case-insensitive in-note search with line numbers and context
      parameters:
        - { name: document_id, in: path, required: true, schema: { type: string } }
        - { name: pattern, in: query, required: true, schema: { type: string } }
      responses:
        '200': { description: Matches }
  /api/v1/notebooks:
    get:
      summary: List all notebooks (flat, with parent IDs)
      responses:
        '200': { description: Notebook list }
    post:
      summary: Create a notebook
      responses:
        '201': { description: Created notebook }
        '409': { description: Sibling name conflict (case-insensitive) }
  /api/v1/notebooks/tree:
    get:
      summary: Nested notebook tree in sidebar order
      responses:
        '200': { description: Notebook tree }
  /api/v1/notebooks/{notebook_id}:
    parameters:
      - { name: notebook_id, in: path, required: true, schema: { type: string } }
    get:
      summary: Read one notebook
      responses:
        '200': { description: Notebook }
    patch:
      summary: Rename/move a notebook or change its emoji/position
      responses:
        '200': { description: Updated notebook }
        '403': { description: Builtin notebooks cannot be renamed or moved }
        '409': { description: Sibling name conflict (case-insensitive) }
    delete:
      summary: Delete a notebook; its notes move to the Trash
      responses:
        '204': { description: Deleted }
        '403': { description: Builtin/default notebooks cannot be deleted }
  /api/v1/notebooks/{notebook_id}/notes:
    get:
      summary: List current notes directly in a notebook
      parameters:
        - { name: notebook_id, in: path, required: true, schema: { type: string } }
        - { name: limit, in: query, schema: { type: integer } }
      responses:
        '200': { description: Documents }
  /api/v1/tags:
    get:
      summary: List tags with live note counts
      responses:
        '200': { description: Tags }
  /api/v1/documents/{document_id}/tags:
    get:
      summary: List one document's tags
      parameters:
        - { name: document_id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: Tags }
  /api/v1/documents/{document_id}/tags/{tag}:
    parameters:
      - { name: document_id, in: path, required: true, schema: { type: string } }
      - { name: tag, in: path, required: true, schema: { type: string } }
    post:
      summary: Add a tag to a document (tag names are case-insensitive)
      responses:
        '200': { description: Tag with updated count }
    delete:
      summary: Remove a tag from a document
      responses:
        '204': { description: Removed; unreferenced tags disappear }
  /api/v1/documents/{document_id}/notebook:
    post:
      summary: Move a document to another notebook
      parameters:
        - { name: document_id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: Updated document }
        '403': { description: Help notes cannot be moved in or out }
  /api/v1/search-notebooks:
    get:
      summary: List search notebooks in sidebar order (All notes first, Trash last)
      responses:
        '200': { description: Search notebooks }
    post:
      summary: Save a query as a search notebook
      responses:
        '201': { description: Created search notebook }
        '409': { description: Name conflict (case-insensitive) }
  /api/v1/search-notebooks/{search_notebook_id}:
    delete:
      summary: Delete a user search notebook (never deletes notes)
      parameters:
        - { name: search_notebook_id, in: path, required: true, schema: { type: string } }
      responses:
        '204': { description: Deleted }
        '403': { description: Builtin search notebooks cannot be deleted }
  /api/v1/trash:
    get:
      summary: List soft-deleted notes (newest deletions first)
      parameters:
        - { name: limit, in: query, schema: { type: integer } }
      responses:
        '200': { description: Trashed documents }
  /api/v1/trash/{document_id}:
    delete:
      summary: Permanently delete a trashed local note
      parameters:
        - { name: document_id, in: path, required: true, schema: { type: string } }
      responses:
        '204': { description: Purged }
        '403': { description: Externally-sourced notes cannot be purged }
  /api/v1/trash/{document_id}/restore:
    post:
      summary: Undelete a trashed note
      parameters:
        - { name: document_id, in: path, required: true, schema: { type: string } }
      responses:
        '200': { description: Restored document }
  /api/v1/graph:
    post:
      summary: Return a local document/resource graph
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GraphRequest'
      responses:
        '200':
          description: Graph
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GraphResponse'
  /api/v1/publish/quartz/plan:
    post:
      summary: Plan a Quartz publishing run without writing files
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PublishPlanRequest'
      responses:
        '200':
          description: Publishing plan
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PublishPlanResponse'
  /api/v1/jobs/{job_id}:
    get:
      summary: Get background job status
      parameters:
        - name: job_id
          in: path
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Job status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/JobStatus'
components:
  parameters:
    CollectionID:
      name: collection_id
      in: path
      required: true
      schema: { type: string }
    DocumentID:
      name: document_id
      in: path
      required: true
      schema: { type: string }
    ResourceID:
      name: resource_id
      in: path
      required: true
      schema: { type: string }
    RevisionID:
      name: revision_id
      in: path
      required: true
      schema: { type: string }
    Limit:
      name: limit
      in: query
      schema: { type: integer, minimum: 1, maximum: 100, default: 25 }
    Cursor:
      name: cursor
      in: query
      schema: { type: string }
    IfMatch:
      name: If-Match
      in: header
      schema: { type: string }
      required: false
  responses:
    Error:
      description: Error response
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorEnvelope'
  schemas:
    StatusResponse:
      type: object
      required: [service, version, status]
      properties:
        service: { type: string }
        version: { type: string }
        status: { type: string, enum: [scaffold, running] }
        database:
          type: string
          description: Deprecated summary retained for early UI compatibility.
        config_path: { type: string }
        database_info:
          type: object
          properties:
            driver: { type: string }
            path: { type: string }
            state: { type: string }
            schema_version: { type: integer }
        storage:
          type: object
          properties:
            data_directory: { type: string }
            database_path: { type: string }
            asset_store: { type: string }
            projection_dir: { type: string }
            search_sidecar_index_dir: { type: string }
        capabilities:
          type: object
          additionalProperties: { type: boolean }
        limits:
          type: object
          additionalProperties: { type: integer }
        media_policy:
          type: object
          description: >
            Active remote-media policy (v0.3): effective default action,
            network limits, and rule counts per configured list. Scanning and
            localization endpoints arrive in later v0.3 tasks.
          properties:
            default_action: { type: string, enum: [allow, block, review] }
            allow_private_networks: { type: boolean }
            max_redirects: { type: integer }
            fetch_timeout_seconds: { type: integer }
            blocked_schemes: { type: integer, description: Count of configured blocked schemes. }
            allowed_domains: { type: integer, description: Count of configured allowed-domain patterns. }
            blocked_domains: { type: integer, description: Count of configured blocked-domain patterns. }
            review_domains: { type: integer, description: Count of configured review-domain patterns. }
            max_bytes:
              type: object
              additionalProperties: { type: integer, format: int64 }
              description: Download size caps in bytes per media class (image/video/pdf).
            quarantine_dir: { type: string }
    ErrorEnvelope:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code: { type: string }
            message: { type: string }
            details: { type: object, additionalProperties: true }
    Collection:
      type: object
      required: [id, name, kind, capabilities]
      properties:
        id: { type: string }
        name: { type: string }
        kind: { type: string, enum: [managed, external, imported, sidecar_indexed, projection] }
        description: { type: string }
        capabilities:
          type: array
          items: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    CollectionPage:
      type: object
      required: [collections]
      properties:
        collections:
          type: array
          items: { $ref: '#/components/schemas/Collection' }
        next_cursor: { type: string, nullable: true }
    CreateCollectionRequest:
      type: object
      required: [id, name, kind]
      properties:
        id: { type: string }
        name: { type: string }
        kind: { type: string }
        description: { type: string }
    PatchCollectionRequest:
      type: object
      properties:
        name: { type: string }
        description: { type: string }
        settings: { type: object, additionalProperties: true }
    SearchRequest:
      type: object
      properties:
        collections:
          type: array
          items: { type: string }
        collection: { type: string, deprecated: true }
        query: { type: string }
        mode: { type: string, enum: [fts, phrase, all_terms, any_terms, semantic, hybrid], default: fts }
        filters: { $ref: '#/components/schemas/SearchFilters' }
        sort:
          type: array
          items: { $ref: '#/components/schemas/SortField' }
        limit: { type: integer, minimum: 1, maximum: 100, default: 25 }
        cursor: { type: string }
        include_total: { type: boolean, default: false }
    SearchFilters:
      type: object
      properties:
        tags:
          type: array
          items: { type: string }
        authors:
          type: array
          items: { type: string }
        date_from: { type: string, format: date-time }
        date_to: { type: string, format: date-time }
        has_media: { type: boolean }
        mime_types:
          type: array
          items: { type: string }
        document_types:
          type: array
          items: { type: string }
        include_deleted: { type: boolean, default: false }
    SortField:
      type: object
      required: [field]
      properties:
        field: { type: string }
        direction: { type: string, enum: [asc, desc], default: asc }
    SearchResponse:
      type: object
      required: [hits]
      properties:
        hits:
          type: array
          items: { $ref: '#/components/schemas/SearchHit' }
        next_cursor: { type: string, nullable: true }
        total: { type: integer, nullable: true }
    SearchHit:
      type: object
      required: [id, uri, source]
      properties:
        id: { type: string }
        uri: { type: string }
        source: { type: string }
        collection_id: { type: string }
        title: { type: string }
        snippet: { type: string }
        score: { type: number }
        editable: { type: boolean }
        metadata: { type: object, additionalProperties: true }
    Document:
      type: object
      required: [id, uri, collection_id, title, body_mime_type, current_revision_id]
      properties:
        id: { type: string }
        uri: { type: string }
        collection_id: { type: string }
        notebook_id: { type: string }
        editable:
          type: boolean
          description: Server-authoritative capability flag; false for protected (Help) and trashed notes. Clients must not infer editability from names.
        title: { type: string }
        body_mime_type: { type: string }
        body: { type: string }
        current_revision_id: { type: string }
        metadata: { type: object, additionalProperties: true }
        tags:
          type: array
          items: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
        deleted_at: { type: string, format: date-time, nullable: true }
    DocumentMutationRequest:
      type: object
      required: [collection_id, title, body]
      properties:
        collection_id: { type: string }
        title: { type: string }
        body: { type: string }
        body_mime_type: { type: string, default: text/markdown }
        base_revision_id: { type: string }
        metadata: { type: object, additionalProperties: true }
        tags:
          type: array
          items: { type: string }
        message: { type: string }
    DocumentPatchRequest:
      type: object
      properties:
        base_revision_id: { type: string }
        title: { type: string }
        metadata: { type: object, additionalProperties: true }
        tags:
          type: array
          items: { type: string }
        edits:
          type: array
          items: { $ref: '#/components/schemas/SurgicalEdit' }
        dry_run: { type: boolean }
    SurgicalEdit:
      type: object
      required: [search, replace]
      properties:
        search: { type: string }
        replace: { type: string }
        fuzzy: { type: boolean, default: false }
    DocumentRevision:
      type: object
      required: [id, document_id, title, created_at]
      properties:
        id: { type: string }
        document_id: { type: string }
        title: { type: string }
        body: { type: string }
        body_mime_type: { type: string }
        metadata: { type: object, additionalProperties: true }
        message: { type: string }
        created_at: { type: string, format: date-time }
    RevisionPage:
      type: object
      required: [revisions]
      properties:
        revisions:
          type: array
          items: { $ref: '#/components/schemas/DocumentRevision' }
        next_cursor: { type: string, nullable: true }
    RestoreRevisionRequest:
      type: object
      properties:
        base_revision_id: { type: string }
        message: { type: string }
    Resource:
      type: object
      required: [id, uri, collection_id, mime_type]
      properties:
        id: { type: string }
        uri: { type: string }
        collection_id: { type: string }
        filename: { type: string }
        mime_type: { type: string }
        size_bytes: { type: integer }
        sha256: { type: string }
        metadata: { type: object, additionalProperties: true }
        created_at: { type: string, format: date-time }
    ResourceReference:
      type: object
      required: [document_id, resource_id, relation_type]
      properties:
        document_id: { type: string }
        resource_id: { type: string }
        resource: { $ref: '#/components/schemas/Resource' }
        relation_type: { type: string }
        ordinal: { type: integer }
        anchor: { type: object, additionalProperties: true }
    ResourceReferencePage:
      type: object
      required: [resources]
      properties:
        resources:
          type: array
          items: { $ref: '#/components/schemas/ResourceReference' }
        next_cursor: { type: string, nullable: true }
    AttachResourceRequest:
      type: object
      properties:
        relation_type: { type: string, default: attachment }
        ordinal: { type: integer, default: 0 }
        anchor: { type: object, additionalProperties: true }
    DocumentLink:
      type: object
      required: [id, source_document_id, relation_type, resolution_status]
      properties:
        id: { type: string }
        source_document_id: { type: string }
        target_document_id: { type: string, nullable: true }
        target_resource_id: { type: string, nullable: true }
        target_uri: { type: string, nullable: true }
        relation_type: { type: string }
        source_format: { type: string }
        raw_target: { type: string }
        display_text: { type: string }
        anchor_type: { type: string }
        anchor_value: { type: string }
        context: { type: string }
        resolution_status: { type: string }
        source_position: { $ref: '#/components/schemas/SourcePosition' }
    SourcePosition:
      type: object
      properties:
        start_byte: { type: integer }
        end_byte: { type: integer }
        line: { type: integer }
        column: { type: integer }
    DocumentLinkPage:
      type: object
      properties:
        outgoing:
          type: array
          items: { $ref: '#/components/schemas/DocumentLink' }
        incoming:
          type: array
          items: { $ref: '#/components/schemas/DocumentLink' }
        next_cursor: { type: string, nullable: true }
    DocumentOutline:
      type: object
      required: [document_id, headings]
      properties:
        document_id: { type: string }
        headings:
          type: array
          items:
            type: object
            required: [level, title]
            properties:
              level: { type: integer }
              title: { type: string }
              anchor: { type: string }
              line: { type: integer }
    RemoteMediaRequest:
      type: object
      properties:
        policy: { type: string }
        types:
          type: array
          items: { type: string }
        urls:
          type: array
          items: { type: string, format: uri }
        dry_run: { type: boolean, default: false }
    RemoteMediaResult:
      type: object
      required: [localized, blocked, review, failed]
      properties:
        revision_id: { type: string }
        localized:
          type: array
          items: { type: object, additionalProperties: true }
        blocked:
          type: array
          items: { type: object, additionalProperties: true }
        review:
          type: array
          items: { type: object, additionalProperties: true }
        failed:
          type: array
          items: { type: object, additionalProperties: true }
    GraphRequest:
      type: object
      required: [roots]
      properties:
        roots:
          type: array
          items: { type: string }
        direction: { type: string, enum: [outgoing, incoming, both], default: both }
        depth: { type: integer, minimum: 0, maximum: 5, default: 1 }
        include_resources: { type: boolean, default: false }
        max_nodes: { type: integer, default: 250 }
        max_edges: { type: integer, default: 500 }
    GraphResponse:
      type: object
      required: [nodes, edges]
      properties:
        nodes:
          type: array
          items: { type: object, additionalProperties: true }
        edges:
          type: array
          items: { type: object, additionalProperties: true }
        truncated: { type: boolean }
    PublishPlanRequest:
      type: object
      properties:
        profile: { type: string }
        include:
          type: object
          additionalProperties: true
        exclude:
          type: object
          additionalProperties: true
        target: { type: string, enum: [quartz], default: quartz }
    PublishPlanResponse:
      type: object
      properties:
        notes_included: { type: integer }
        resources_included: { type: integer }
        notes_excluded: { type: integer }
        warnings:
          type: array
          items: { type: string }
    JobStatus:
      type: object
      required: [id, kind, status]
      properties:
        id: { type: string }
        kind: { type: string }
        status: { type: string }
        progress: { type: number }
        message: { type: string }
